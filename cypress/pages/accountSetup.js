import accountSetup from '../test-data/accountSetup.json'

const nameOfAppLocator = 'input[id="name"]'
const appIDLocator = 'input[id="app_id"]'
const appTokenLocator = 'input[id="app_token"]'

class accountSetup{
    submitAccountSetup(){
        
    }
}

module.exports = new accountSetup()